name = "mybirthday-myx-is"
main = "worker.ts"
compatibility_date = "2024-12-01"

# Optional: custom domain
# routes = [{ pattern = "mybirthday.myx.is", custom_domain = true }]

[rules]
  - { type = "Text", globs = ["**/*.html"] }
